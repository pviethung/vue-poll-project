name: Build and Test
on:
  push:
    branches: ["develop"]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install deps
        run: yarn
      - name: Build
        run: yarn run build
      - name: Test
        run: yarn run test
      - run: echo "Done!"
